akka-cc {
  akka {
    # log-config-on-start = "on"
    actor {
      provider = cluster
    }
    remote {
      log-remote-lifecycle-events = off
      netty.tcp {
        hostname = "local.particeep.com"
        port = 2551

        bind-hostname = "127.0.0.1" # internal (bind) hostname
        bind-port = 2551
      }
    }
    cluster {
      seed-nodes = [
        #"akka.tcp://akka-cc@test-cluster.particeep.com:80"
        #"akka.tcp://akka-cc@127.0.0.1:2551"

        #"akka.tcp://akka-cc@local.particeep.com:2551"
        "akka.tcp://akka-cc@test-cluster-node.particeep.com:80"
        "akka.tcp://akka-cc@aac280b2.ngrok.io:80"
      ]

      # auto-down-unreachable-after = 10s
    }
  }

  akka.log-dead-letters=off

  akka.persistence.journal.plugin = "akka.persistence.journal.leveldb"
  akka.persistence.journal.leveldb.dir = "target/journal22"

  # Disable legacy metrics in akka-cluster.
  akka.cluster.metrics.enabled=off

  # Enable metrics extension in akka-cluster-metrics.
  akka.extensions=["akka.cluster.metrics.ClusterMetricsExtension"]

  # Sigar native library extract location during tests.
  # Note: use per-jvm-instance folder when running multiple jvm on one host.
  # akka.cluster.metrics.native-library-extract-folder=${user.dir}/target/native

}
